# 커맨드센터 이관 패키지 (2026-03-25 09:20)

## 세션 요약
커맨드센터: 맥스튜 (Claude Code VSCode Opus)
시작: /tmp/migration_package.md → Worker 재생성 → 메일 폴링 검증
총 커밋: 약 15건 (main)

## 핵심 수정 사항

### 완료
1. asyncio event loop 충돌 해결 (_run_async + engine.dispose)
2. RFC 2822 날짜 파싱 (received_at str→datetime)
3. 스케줄 DB 통합 (DatabaseScheduler, beat_schedule 제거, 7개 시딩)
4. 화이트리스트 전환 (support 주소로 온 외부 메일만 AI 처리)
5. via 포워딩 처리 (from_name에 via가 있으면 외부 경유로 인식)
6. via 시스템 vs 고객 구분 (스마트스토어/Steam=ignored, 김해미/정해찬=open)
7. status 체계: processing/open/replied/done/ignored
8. ignored 메일 UI 필터 (list_emails에서 기본 제외)
9. 사이드바 무시됨 폴더 추가 + 카운트 수정
10. 스팸 룰 매칭 → ignored 처리
11. restart:unless-stopped 전체 컨테이너
12. Redis seen-cache 폴링 폭주 방지
13. 중복 메일 upsert 처리

### 미해결 (버그큐 참조)
- B7: 와치독/헬스체크 미구현
- 챗봇 뱃지 256건 누적 (proactive_notifications 중복 생성)
- received 351건 상태 정리 필요
- 룰 정밀화 (중복 룰 제거, OR→AND)
- 지식문서 보강 (confidence 향상)
- HTML 본문 깨짐 (일부 메일)

## 현재 상태
- 폴링: 2분마다 정상 (DB 스케줄러, Beat Up 13h)
- 파이프라인: 화이트리스트 + via 감지 + 시스템 필터
- CI: ruff check 통과
- status 분포: received 351, done 112, ignored 89, open 4, replied 1

## 팀 태스크 (배정 완료, 응답 대기)
- PL: 메일 분류 체계 + status 체계 기획
- TL: Gmail 포워딩 아키텍처 리뷰 + 와치독 설계
- Coder: via 패턴 발신자 식별 분석 + 룰 정밀화
- Reviewer: pipeline.py 전수 점검 + 오늘 커밋 리뷰
- Tester: 현재 상태 기록 + 화이트리스트 E2E 검증
(브릿지 안 돌고 있어 응답 없음 — 브릿지 재시작 필요)

## 아카샤 저장 문서
- 버그큐 — 2026-03-24 세션 보고 (B1~B10)
- B3 분석 — via 포워딩 메일 오판 상세
- Claude Team Bible v2.0 — 클론 구축 매뉴얼
- Claude Team - Slack 전체 토큰 (Bot + App)

## 사용자 확정 사항
- 모든 메일 반응 X → 특정 메일만 반응
- 블랙리스트 X → 화이트리스트
- 스킵 메일 → status=ignored, UI에서 필터 + 무시됨 폴더에서 조회 가능
- 내부 발신 → status=replied
- via 고객 → AI 처리, via 시스템 → ignored

## 인프라
- EC2: 3.34.66.166
- SSH: ssh -i ~/.ssh/auto-email-poc.pem ec2-user@3.34.66.166
- GitHub PAT: ghp_***MASKED***
- 아카샤: wooho.yang@tbnws.com PIN ****

## 다음 세션 할 일
1. EC2 pull (admin 프론트엔드는 docker rebuild 필요)
2. 브릿지 재시작 → 팀 응답 수집
3. received 351건 상태 정리
4. 와치독 구현
5. 룰 정밀화 + 지식문서 보강
6. 챗봇 알림 중복 방지
