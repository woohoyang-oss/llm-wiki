# 커맨드센터 이관 패키지 (2026-03-25 12:00)

## 세션 패치 내역

### 1. 룰 액션에 "무시(ignore)" 옵션 추가
- `pipeline.py`: ignore 액션 → 즉시 status=ignored, AI 처리 스킵
- `create-rule-from-email.tsx`, `rule-form-dialog.tsx`: 드롭다운에 "무시" 추가
- `rules/page.tsx`: ACTION_CONFIG에 ignore 표시 (빨간 배지, EyeOff 아이콘)

### 2. 스팸/광고/영업 무시 룰 3개 등록
- `spam-marketing-ads`: 인플루언서, 바이럴, 리워드재활센터, 뉴스레터, 히든태그, HKTDC, iPega, hkpega.com
- `system-notice-ignore`: 작업공지, SSL, 출고일 조정, coupang.com, danal.co.kr
- `b2b-partnership-ignore`: 파트너쉽 제안, trans-cosmos.co.kr

### 3. 처리완료 메일함 빈 페이지 버그 수정
- `emails/page.tsx`: TEAM_FOLDER_STATUS의 `sent: "solved"` → `sent: "done"`
- DB에는 done으로 저장되는데 solved로 필터해서 0건이었음

### 4. AI 응답 테이블 렌더링 수정
- `ai_processor.py`: 두 프롬프트 모두에 HTML 테이블 규칙 추가 (마크다운 테이블 금지)
- `globals.css`: `.email-html-content table/th/td` CSS 추가 (border, padding, zebra stripe, 다크모드)

### 5. 의견주기 답변 저장 시 유실 버그 수정
- `models/base.py`: `ai_responses` relationship에 `order_by="AIResponse.id.desc()"` 추가
- 원인: bestAI = ai_responses[0]이 항상 가장 오래된 응답을 반환

### 6. bridge.py Claude CLI 경로 + Keychain 자동 unlock
- CLAUDE_BIN 자동 탐색 (shutil.which + fallback 경로)
- CLI 호출 전 macOS Keychain 자동 unlock (KEYCHAIN_PASSWORD 환경변수)
- SSH 세션 종료 후에도 안정적 동작

### 7. 뉴스레터함 사이드바 추가
- `app-sidebar.tsx`: teamFolders에 뉴스레터(Newspaper 아이콘) 추가
- `emails/page.tsx`: TEAM_FOLDER_STATUS에 `newsletter: "newsletter"` 매핑
- 사이드바 카운트에서 newsletter 별도 집계

### 8. 스팸지수 + 뉴스레터 감지 (Coder 구현)
- `pipeline.py`: `_calculate_spam_score()` — 키워드/발신자/unsubscribe/HTML비율/URL 수 기반 0~100점
- `pipeline.py`: `_is_newsletter()` — List-Unsubscribe 헤더 or 뉴스레터 키워드
- 뉴스레터 → status='newsletter', 스팸≥60 → status='ignored' + spam-detected 태그

## 팀 상태
- TL: ✅ 온라인 (맥북)
- PL: ✅ 온라인 (맥북) — keychain auto-unlock 적용
- Coder: ✅ 온라인 (맥미니) — 스팸지수+뉴스레터 구현 완료
- Reviewer: ✅ 온라인 (맥미니) — 오늘 커밋 LGTM
- Tester: ⏳ 대기 (맥미니) — Coder 작업 검증 대기

## 인프라 변경
- EC2 admin 프론트 리빌드 완료 (--no-cache)
- 맥미니: python-dotenv, slack-bolt, slack-sdk pip 설치
- 맥미니/맥북: KEYCHAIN_PASSWORD를 .env.claude-team에 추가

## 미해결
- PL 파이프라인 점검: Errno 2로 미완료 → 재시작 후 재지시 필요
- Tester 검증: Coder 스팸지수 구현 검증 대기
- received 327건 상태 정리
- 와치독/헬스체크 미구현
- proactive_notifications 재설계